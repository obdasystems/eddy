# Requirements for packaging the application

# Recursive
-r base.in

# From PyPi
appimage-builder==1.1.0; sys_platform == 'linux'
dmgbuild==1.6.1; sys_platform == 'darwin'
Pillow==9.5.0; sys_platform == 'darwin'
PyInstaller==5.13.0
PyYAML==6.0; sys_platform == 'win32'
