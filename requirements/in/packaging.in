# Requirements for packaging the application

# Recursive
-r base.in

# From PyPi
appimage-builder==0.9.2; sys_platform == 'linux'
dmgbuild==1.5.2; sys_platform == 'darwin'
Pillow==9.0.1; sys_platform == 'darwin'
PyInstaller==4.7
PyYAML==6.0; sys_platform == 'win32'
